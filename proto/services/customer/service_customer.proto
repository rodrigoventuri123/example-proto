syntax = "proto3";

package customers;

option go_package = "customerspb";

import "services/customer/messages_customer.proto";

service CustomerAPI {
  rpc AddCustomer (customers.AddCustomerRequest) returns (customers.AddCustomerResponse);
}
